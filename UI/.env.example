# Optional: Flask ML API (see ../api/). When unset, Next.js uses a mock predictor.
# Example local: FLASK_API_URL=http://127.0.0.1:5000
# Vercel: set the same env var to your deployed Flask base URL (no trailing slash).
FLASK_API_URL=http://127.0.0.1:5000
